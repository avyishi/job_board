= simple_form_for(@job, html: { class: 'form-hortizontal'}) do |f|
  = f.collection_select :category_id, Category.all, :id, :name, {promt: "Choose a job category"}
  = f.input :title, label: "Job Titie" 
  = f.input :description, label: "Job Description" 
  %br/
  = f.button :submit